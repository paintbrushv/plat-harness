# market-study stub

Rent-roll standardization and comps scraping are **not** copied in this first
seed. Live property YAML, report trees, paid data, and any CSV with
`resident_name` stay out.

Later copy (second audit): standardizer + scrape engines + four-pack emit, with
**fabricated** fixtures only.

Until then: missing comps four-pack → rent upside `NEEDS_DATA`. This stub does
not invent asking rents.
